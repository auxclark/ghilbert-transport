# URL of your deployed backend (Render), no trailing slash.
# Leave unset for local dev — Vite's dev server proxies /api and /uploads to localhost:5000 instead.
VITE_API_URL=https://your-backend-name.onrender.com
